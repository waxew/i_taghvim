package com.asteam.itaghvim.core.ads

/**
 * تنظیمات عمومی ماژول تبلیغات آی تقویم.
 *
 * این فایل عمداً از SDK تپسل مستقل است تا تنظیمات تبلیغات
 * در یک نقطه مدیریت شود و UI مستقیماً به SDK وابسته نباشد.
 */
object AdSettings {

    const val ENABLE_BANNER = true
    const val ENABLE_REWARDED = true
    const val ENABLE_INTERSTITIAL = true

    const val BANNER_REFRESH_ENABLED = true
}
